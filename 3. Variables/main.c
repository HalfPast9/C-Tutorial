#include <stdio.h>
#include <stdlib.h>

int main()
{   

    // must be declared befoe use
    // can be initialized at declaration
    // variables never empty, they always have a value just dont know what it is until you assign it
    // varible data types cannot be changed after declaration
    
    char characterName[] = "John";
    int characterAge = 35;
    printf("wahhhhhhhh %s\n", characterName);
    printf("BAHHHHHHHH %d HHHHHH\n", characterAge);
    return 0;
}