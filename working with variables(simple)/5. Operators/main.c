#include <stdio.h>
#include <stdlib.h>

int main()
{  
   int num1 = 6;
   int num2 = 4;
   int num3;
   //****ARITHMETIC OPERATORS****
   // +, -, *, /, %, ++, --

   // % is the remainder operator

   // / is integer division, if you want decimal division, use float or double data types
   // yields an integer result when both operands are integers
   // yields a float result when one or both operands are floats

   // both / & % round towards 0(if answer negative, round down, if answer positive, round up)


   // ++ and -- are increment and decrement operators
   

   //****ASSIGNMENT OPERATORS****
   // =, +=, -=, *=, /=, %=, <<=, >>=, &=, ^=, |=
   // x += 5 is the same as x = x + 5
   // x <<= 3 is the same as x = x << 3
   // x &= 3 is the same as x = x & 3
   // x ^= 3 is the same as x = x ^ 3
   // x |= 3 is the same as x = x | 3


   //****RELATIONAL OPERATORS****
   // ==, !=, >, <, >=, <=
   // returns 1 if true, 0 if false


   //****LOGICAL OPERATORS****
   // &&(and), ||(or), !(not)

   
   // returns 1 if true, 0 if false

   return 0;   
}